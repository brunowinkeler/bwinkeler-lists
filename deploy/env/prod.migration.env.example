# Migration job for lists-api (migrator role). Real values live in
# /etc/bwinkeler/apps/lists/migration.env on the host, never in Git.
NODE_ENV=production
PGHOST=bw-postgres
PGPORT=5432
PGDATABASE=lists_db
PGUSER=lists_migrator
PGPASSWORD=set-in-secret-file
